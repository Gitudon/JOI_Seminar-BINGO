version: '3.8'
services:
  bot:
    build: .
    volumes:
      - ./bot:/usr/src/bot:rw
    env_file:
      - .env
    environment:
      - TZ=Asia/Tokyo
    command: ["python3", "seminar_bingo.py"]
    tty: true
